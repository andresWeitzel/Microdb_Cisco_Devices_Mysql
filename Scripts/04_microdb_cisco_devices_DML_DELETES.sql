/* -------------------------------------
 * ------ MICRODB CISCO DEVICES---------
 * -------------------------------------
 * 
 * 
 * ========= DML DELETES =============
 */

-- DATABASE
use microdb_cisco_devices;

-- VARS
SET @created_at = now();
SET @updated_at = now();
set @today = now();